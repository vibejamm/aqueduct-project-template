test('should [do something] when [user does action]', async ({ page }) => {
  // Given: [Starting state - what page/data are we starting with?]
  
  // When: [User action - what does the user do?]
  
  // Then: [Expected result - what should happen?]
  
  // Example structure:
  // await page.goto('/dashboard');
  // await page.click('button:has-text("Create Item")');
  // await page.fill('input[name="title"]', 'Test Item');
  // await page.click('button:has-text("Save")');
  // await expect(page.locator('text=Test Item')).toBeVisible();
});